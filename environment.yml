name: self-halt
channels:
  - defaults
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
dependencies:
  - jupyter
  - yaml
  - pandas
  - itables
  - matplotlib
prefix: /opt/anaconda3/envs/self-halt
